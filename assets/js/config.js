/**
 * MediCore — App Configuration
 *
 * Change BACKEND_URL to your deployed server URL in production.
 * Example: "https://api.medicore.example.com"
 */
export const CONFIG = {
  BACKEND_URL: "http://localhost:3001",
};
